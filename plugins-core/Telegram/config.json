{
    "name": "Telegram Bot Integration",
    "code": "telegram",
    "version": "2.1.0",
    "description": "Multilingual Telegram bot with menus, notifications, node reports, and reseller tools.",
    "author": "XBoard Team",
    "require": {
        "xboard": ">=1.0.0"
    },
    "config": {
        "enable_ticket_notify": {
            "type": "boolean",
            "default": true,
            "label": "Enable ticket notifications",
            "description": "Send new tickets and customer replies to administrators and staff."
        },
        "enable_payment_notify": {
            "type": "boolean",
            "default": true,
            "label": "Enable payment notifications",
            "description": "Send successful payment notifications to administrators and staff."
        },
        "enable_node_group_report": {
            "type": "boolean",
            "default": false,
            "label": "Send scheduled node reports to a group",
            "description": "An administrator can use /setreportgroup in a group to select the report destination."
        },
        "node_report_interval_minutes": {
            "type": "select",
            "default": "15",
            "label": "Node report interval",
            "description": "Minutes between node reports.",
            "options": [
                {"value": "5", "label": "5 minutes"},
                {"value": "15", "label": "15 minutes"},
                {"value": "60", "label": "60 minutes"}
            ]
        },
        "enable_database_backup": {
            "type": "boolean",
            "default": false,
            "label": "Send automatic database backups through Telegram",
            "description": "Create, compress, and encrypt a database backup daily, then send it to the private chat selected with /setbackupchat."
        },
        "database_backup_time": {
            "type": "string",
            "default": "03:30",
            "label": "Database backup time",
            "description": "Server time in HH:MM format."
        },
        "database_backup_chat_id": {
            "type": "string",
            "default": "",
            "label": "Backup destination chat ID (optional)",
            "description": "Leave blank and send /setbackupchat to the bot in a private chat."
        },
        "database_backup_password": {
            "type": "password",
            "default": "",
            "label": "Backup encryption password",
            "description": "At least 16 characters. Prefer TELEGRAM_DATABASE_BACKUP_PASSWORD in .env instead of saving it here."
        },
        "database_backup_max_mb": {
            "type": "select",
            "default": "45",
            "label": "Maximum backup size",
            "description": "Stop and report an error if the backup exceeds the selected limit.",
            "options": [
                {"value": "20", "label": "20 MB"},
                {"value": "45", "label": "45 MB"}
            ]
        },
        "enable_reseller_bot": {
            "type": "boolean",
            "default": false,
            "label": "Enable reseller tools",
            "description": "Allow administrators, staff, and specified Telegram IDs to create accounts using a 100% coupon."
        },
        "reseller_telegram_ids": {
            "type": "string",
            "default": "",
            "label": "Authorized reseller Telegram IDs",
            "description": "Comma-separated Telegram IDs that are allowed to use reseller tools."
        }
    }
}
